# 本地存储恢复

一个数据目录仅供一个服务进程使用；写入队列和内存缓存不跨进程协调。

本地写入使用数据目录内的 `.navilink-tx-*` 事务目录。存在 `pending.json` 表示尚未提交，启动时先从备份恢复旧数据；存在 `committed.json` 表示数据已提交，只需清理事务目录。恢复完成前不提供本地数据读写。

回滚失败时保留事务目录并阻止后续本地读写。排除磁盘空间、权限或文件占用问题后重启服务执行恢复。不要手动删除未恢复的事务目录，否则可能失去原始数据。

该机制处理进程中断，不承诺断电或文件系统损坏后的跨文件事务持久性。数据目录仍需要独立备份。
