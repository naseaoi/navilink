# 性能、架构与运行优化待办

## 批次一：首屏链路

- [x] 为 Express 静态资源启用 gzip/Brotli 协商压缩，验证响应包含 `Content-Encoding`
- [x] 为公开数据增加服务端短时缓存与明确的公共缓存策略，写入后立即失效/更新
- [x] 公开数据采用本地缓存先展示、远端后台刷新的加载方式，API 异常时保留可用缓存
- [x] 仅访问后台路由时校验会话，补齐 Vercel `login/verify/logout` 兼容端点
- [x] 补充公开数据缓存与认证端点测试，通过 lint、单测、类型检查和生产构建

## 批次二：图标链路

- [ ] 图标进入可视区域后才访问 IndexedDB 或代理接口
- [ ] 相同图标 URL 的并发请求合并为一次，避免原地址与代理地址双重请求
- [ ] 处理属性快速变化造成的异步竞态，并限制会话内 Object URL 数量
- [ ] 通过 lint、单测、类型检查和生产构建

## 批次三：数据一致性

- [ ] 本地写入使用唯一临时文件，并通过写队列串行化同一存储服务的修改
- [ ] 将版本读取、冲突校验和批量条件写入合并为存储层事务
- [ ] Express WebDAV 使用版本校验时读取到的 ETag，不在写入前重新接受新版本
- [ ] 补充并发保存测试，验证同一版本仅允许一次保存成功
- [ ] 通过 lint、单测、类型检查和生产构建

## 批次四：运行与架构收尾

- [ ] 后台存储状态仅在进入存储页时加载，独立数据读取并发执行
- [ ] 为 Express/Vercel 认证入口补充统一契约测试
- [ ] 前端构建依赖移出 Docker 生产依赖，容器使用非 root 用户
- [ ] 增加健康检查端点、Docker HEALTHCHECK 和 SIGTERM 优雅退出
- [ ] 移除首屏第三方字体请求，收紧对应 CSP
- [ ] 更新部署文档，通过全量构建和 Chromium E2E

## 最终验收

- [ ] 所有待办已完成
- [ ] `npm run build` 通过
- [ ] `npm run test:e2e` 通过
- [ ] 每个批次均有独立 Git 提交
